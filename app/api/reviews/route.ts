import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { reviewSchema } from "@/lib/validation";
export async function POST(request: Request) { const parsed = reviewSchema.safeParse(await request.json()); if (!parsed.success) return NextResponse.json({ error: "Перевірте оцінку та текст відгуку" }, { status: 400 }); const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return NextResponse.json({ error: "Потрібен вхід" }, { status: 401 }); const { data: existing } = await supabase.from("reviews").select("id").eq("author_id", user.id).eq("tutor_id", parsed.data.tutorId).is("deleted_at", null).maybeSingle(); const values = { tutor_id: parsed.data.tutorId, author_id: user.id, rating: parsed.data.rating, body: parsed.data.body }; const { error } = existing ? await supabase.from("reviews").update(values).eq("id", existing.id) : await supabase.from("reviews").insert(values); return error ? NextResponse.json({ error: error.message }, { status: 400 }) : NextResponse.json({ ok: true }); }
