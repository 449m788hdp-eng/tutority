"use client";
import { useState } from "react";
export function ModerationActions({ tutorId }: { tutorId: string }) { const [busy, setBusy] = useState(false); const [message, setMessage] = useState<string | null>(null); async function act(decision: string) { setBusy(true); const response = await fetch("/api/admin/moderation", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ tutorId, decision }) }); setMessage(response.ok ? "Зміни збережено" : "Не вдалося виконати дію"); setBusy(false); } return <div className="pills"><button disabled={busy} className="button primary" onClick={() => act("approved")}>Схвалити</button><button disabled={busy} className="button" onClick={() => act("changes_requested")}>Запросити зміни</button><button disabled={busy} className="button danger" onClick={() => act("rejected")}>Відхилити</button>{message && <span className="muted">{message}</span>}</div>; }
